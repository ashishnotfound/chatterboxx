const fs = require('fs');
const { createCanvas } = require('canvas');

// If canvas is not available, we'll skip this
console.log('Icon generation script - requires canvas package');
console.log('Run: npm install canvas');
console.log('Or use the HTML generator in your browser');
